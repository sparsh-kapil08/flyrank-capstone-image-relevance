# Gemini API key from Google AI Studio (aistudio.google.com)
# Free tier - no credit card needed.
# If not set, the app runs fully offline with deterministic fallback classification.
GEMINI_API_KEY=your_gemini_api_key_here

# Port the Express server listens on. Defaults to 3000 if not set.
PORT=3000
